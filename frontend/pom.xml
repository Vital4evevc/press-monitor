<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>com.ourcrowd</groupId>
    <artifactId>press-monitor-frontend</artifactId>
    <version>1.0.0</version>
    <name>press-monitor-frontend</name>
    <description>
        Serves the static dashboard and the same /api/* surface as press-monitor-backend.
        Read-only endpoints (companies, mentions, summary, timeline) query MySQL directly
        through the shared DashboardService/repositories in press-monitor-shared, using a
        read-only DB user. Pipeline triggering (/api/run) and the Ollama health check
        (/api/health) are backend-only concerns and stay proxied over HTTP.
    </description>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>
        <!-- Shared wire-contract DTOs/enums with press-monitor-backend.
             Must be `mvn install`ed from ../shared-library first — see README. -->
        <dependency>
            <groupId>com.ourcrowd</groupId>
            <artifactId>press-monitor-shared</artifactId>
            <version>1.0.0</version>
        </dependency>

        <!-- Serves the static dashboard + hosts the proxy REST controllers -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Persistence: JPA + MySQL, for querying companies/mentions directly (read-only
             credentials — see application.yml). Same starter as the backend; the entities
             and repositories themselves live in press-monitor-shared. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Bean validation for @ConfigurationProperties -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.springframework.boot</groupId>
                            <artifactId>spring-boot-configuration-processor</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
